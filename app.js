var Header = React.createElement("h1", null, "Hello World");
  // first argument is the html element we want to create. Second argument is eleemnts attributes.
  // The third argument is the element element children. It is the content or value between the opening and closing tags.
ReactDOM.render(Header, document.getElementById('root'));
  // The first element is the code with element we want to pass.
  // The second element is where you pass the node element.

ReactDOM.render(Header, document.querySelector('.Klass'));



